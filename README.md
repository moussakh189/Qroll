# Qroll
QRoll is a Java application that automates attendance tracking using QR codes. Professors generate time‑based one‑time passwords (TOTP) per session; students scan the code to mark their presence. Data is stored locally in an SQLite database.
